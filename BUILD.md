# ساختِ نصب‌کننده‌ها (Windows · macOS · Linux)

اپ کراس‌پلتفرمه، ولی هر نصب‌کننده باید روی همان سیستم‌عامل (یا CI آن) ساخته شود.
(از لینوکس نمی‌شود نصبِ ویندوز/مک ساخت — ابزارِ بومی لازم است.)

## راهِ راحت: GitHub Actions (پیشنهادی — بدون نیاز به سیستمِ بیلد)

۱. پروژه را در یک ریپوی GitHub بگذار.
۲. یک تگ بزن:
   ```bash
   git tag v0.1.0 && git push --tags
   ```
۳. Action خودکار روی ویندوز، مک و لینوکس بیلد می‌گیرد و فایل‌ها را در **Releases** می‌گذارد:
   - `Kateb-Setup-0.1.0.exe` ← نصبِ دابل‌کلیکیِ ویندوز
   - `Kateb-0.1.0.dmg` ← مک، و `Kateb-0.1.0.AppImage` ← لینوکس

   (یا از تبِ **Actions → run → Artifacts** دانلود کن.)

## ساختِ محلی (روی همان سیستم‌عامل)

```bash
npm install
npm run dist:win     # روی ویندوز → Kateb-Setup.exe (نصبِ دابل‌کلیکی)
npm run dist:mac     # روی مک → dmg.
npm run dist:linux   # روی لینوکس → AppImage. (دابل‌کلیکی)
```

## نکته‌ها

- **بدونِ امضای دیجیتال:** بیلدها امضا نشده‌اند. ویندوز ممکن است SmartScreen و مک Gatekeeper
  هشدار بدهد؛ کاربر «Run anyway / Open» را بزند. برای توزیعِ داخلیِ موسسه کافی است. (برای امضا،
  گواهی Windows/Apple لازم است.)
- **Chromium:** داخلِ نصب‌کننده باندل نمی‌شود؛ اپ در **اولین اجرا** خودش دانلودش می‌کند
  (نصب سبک می‌ماند). پس اولین اجرا کمی طول می‌کشد.
- **مک:** فقط روی خودِ مک یا macOS runnerِ Actions ساخته می‌شود.
